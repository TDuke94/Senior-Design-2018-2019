using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class SolfegeDropDown : MonoBehaviour {

	public Dropdown sol;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		//print(sol.options[sol.value].text);

		//PlayerPrefs.SetString("dropdown",sol.options[sol.value].text);

	}
}
